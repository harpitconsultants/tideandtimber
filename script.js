const buttons=document.querySelectorAll('[data-filter]');const cards=document.querySelectorAll('[data-type]');buttons.forEach(button=>button.addEventListener('click',()=>{buttons.forEach(b=>b.classList.remove('active'));button.classList.add('active');const filter=button.dataset.filter;cards.forEach(card=>card.classList.toggle('hidden',filter!=='all'&&card.dataset.type!==filter));}));
const dialog=document.querySelector('#stay-dialog');const title=document.querySelector('#dialog-title');const link=document.querySelector('#dialog-link');document.querySelectorAll('[data-property]').forEach(button=>button.addEventListener('click',()=>{title.textContent=button.dataset.property;link.href=`mailto:hello@example.com?subject=${encodeURIComponent('Inquiry about '+button.dataset.property)}`;dialog.showModal();}));document.querySelector('.close').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',event=>{if(event.target===dialog)dialog.close();});
